URGENT FIX — DO NOT MAKE ANOTHER UI REDESIGN

Look at the current Arabesque White theme implementation.

There is STILL a major bug with the central counter.

The screenshot shows:

• The LCD display is blank.
• The number “030” is floating ABOVE the LCD.
• The number is clearly being rendered as a separate text element.
• The counter itself is being treated as a decorative image/object instead of the actual functional counter component.

THIS MUST BE FIXED.

--------------------------------------------------
1. THE COUNTER MUST BE THE ACTUAL FUNCTIONAL COUNTER
--------------------------------------------------

Do NOT use a decorative image/mockup of a counter with a separate number over it.

The central tasbeeh must remain the EXISTING FUNCTIONAL COUNTER COMPONENT used by the original app.

Reuse the existing counter component and its existing count/state logic.

ONLY change its visual appearance for Arabesque White.

Do NOT create a second counter component.

Do NOT overlay a separate count text on top of the counter.

--------------------------------------------------
2. THE NUMBER MUST APPEAR INSIDE THE LCD
--------------------------------------------------

This is the MOST IMPORTANT FIX.

Currently the screen looks like:

        030   ← WRONG
      ┌───────┐
      │       │
      │ BLANK │
      └───────┘

It must look like:

      ┌───────────────┐
      │      030      │
      └───────────────┘

The count must be rendered INSIDE the actual LCD/display area of the counter.

There must be ZERO floating count text above the LCD.

Remove the incorrectly positioned overlay count.

--------------------------------------------------
3. USE THE EXISTING COUNT STATE
--------------------------------------------------

Do NOT hardcode:

030
034
100

The LCD must display the real application counter value.

For example:

count = 30
→ LCD displays 030

count = 34
→ LCD displays 034

count = 100
→ LCD displays 100

Use the EXISTING counter state/logic already implemented in the application.

When the user taps the physical counter button:

• Existing count logic executes
• Existing state updates
• LCD updates immediately
• Existing sound behavior remains
• Existing vibration behavior remains
• Existing history/statistics behavior remains

--------------------------------------------------
4. DO NOT CHANGE THE COUNTER'S LAYOUT OR SIZE
--------------------------------------------------

The current counter size is now much closer to what we need.

KEEP IT.

Do NOT make it smaller.

Do NOT make it larger.

Do NOT move it to another location.

Use the existing/default theme's counter dimensions as the source of truth.

The Arabesque White theme should use the SAME:

• Counter width
• Counter height
• Counter position
• LCD position
• LCD size
• Main button position
• Main button size
• Side button position
• Overall proportions

Only the MATERIAL and VISUAL DESIGN should change.

--------------------------------------------------
5. DO NOT USE AN IMAGE AS THE COUNTER
--------------------------------------------------

Very important:

Do NOT implement the counter as:

Image of counter
+
HTML/Text overlay showing count

Instead:

Existing functional counter component
+
Arabesque White styling

The LCD must be a real UI element inside the counter component.

The count must be a child of the LCD/display container.

Conceptually:

Counter
 ├── LCD Display
 │    └── Actual Count
 ├── Main Physical Button
 └── Side Button

NOT:

Counter Image
+
Floating Count Text

--------------------------------------------------
6. PRESERVE ALL EXISTING COUNTER CONTROLS
--------------------------------------------------

Do NOT remove existing controls just because the theme changed.

Keep all controls currently available in the original app, including where applicable:

• Sound
• Vibrate
• Reset
• Bookmark
• Dhikr selector
• Counter interaction

Their position and functionality should remain consistent with the original app.

The theme should not hide or remove controls.

--------------------------------------------------
7. DO NOT CHANGE THE EXISTING SCREEN STRUCTURE
--------------------------------------------------

Keep the existing layout exactly.

The screen should continue to contain:

TOP:
Tasbeeh
Settings

THEN:
Dhikr/Bismillah selector

CENTER:
Large functional tasbeeh counter

BELOW:
Quran quote

BOTTOM:
Existing bottom navigation:

Tasbeeh
History
Stats
Dhikrs

Do NOT create a new layout.

Do NOT move these sections around.

--------------------------------------------------
8. ARABESQUE WHITE SHOULD ONLY CHANGE THE VISUAL SKIN
--------------------------------------------------

Keep the existing component structure and apply:

COUNTER BODY:
• Ivory white marble
• Subtle natural marble veins
• Fine gold Arabesque ornamentation
• Antique-gold trim

BUTTON:
• Brushed gold
• Realistic metallic texture

LCD:
• Same LCD dimensions as the original theme
• Same functional display
• Dark/grey LCD background
• Black digital digits

The LCD must remain readable.

--------------------------------------------------
9. IMPORTANT: INSPECT THE EXISTING CODE FIRST
--------------------------------------------------

Before making changes:

Find the existing working counter component used by the original/default theme.

Identify:

• Where the count state is stored
• Where the count is rendered
• The existing LCD/display element
• The existing counter dimensions
• Existing button interaction
• Existing sound/vibration logic

Then reuse that component.

Do NOT create a new implementation.

If the existing counter already has a working LCD/count rendering system, DO NOT replace it.

Only apply Arabesque White styles/classes to it.

--------------------------------------------------
10. REMOVE THE CURRENT BUGGED OVERLAY
--------------------------------------------------

Find and REMOVE the code responsible for rendering the floating:

“030”

above the LCD.

There should be exactly ONE count renderer.

The final DOM/component hierarchy should have the count INSIDE the LCD container.

There must NOT be:

position: absolute

or similar positioning used to place the count independently over the counter image.

Do not use a separate overlay for the counter value.

--------------------------------------------------
11. FINAL ACCEPTANCE TEST
--------------------------------------------------

Before considering this task complete, verify all of the following:

✓ Counter is the same size as the original/default theme
✓ Counter is in the same position
✓ LCD is visible
✓ Count appears INSIDE LCD
✓ No count appears outside the LCD
✓ Only ONE count is rendered
✓ Count uses the actual application state
✓ Tapping the counter increments the count
✓ LCD updates after tapping
✓ Sound still works
✓ Vibration still works
✓ Reset still works
✓ Bookmark still works
✓ History still works
✓ Stats still works
✓ Dhikr selection still works
✓ Existing navigation is unchanged
✓ Existing themes are unchanged
✓ Arabesque White only changes visual styling

MOST IMPORTANT:

DO NOT FIX THIS BY MOVING THE “030” TEXT.

The solution is NOT to reposition the text.

The solution is to REMOVE the separate overlay text and render the actual counter value INSIDE THE EXISTING LCD COMPONENT.

Use the existing working counter implementation as the source of truth.

THEME = VISUAL SKIN ONLY.
FUNCTIONAL COUNTER = EXISTING COMPONENT.
COUNT = INSIDE EXISTING LCD.