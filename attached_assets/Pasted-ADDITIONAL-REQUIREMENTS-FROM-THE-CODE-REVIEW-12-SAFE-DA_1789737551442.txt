ADDITIONAL REQUIREMENTS FROM THE CODE REVIEW

12. SAFE DATA MIGRATION
- The app already has logic for loading older saved data and filling in missing fields.
- Extend that existing migration/default-loading approach for the new target and statistics data.
- Existing users must not lose their current counts, history, settings, or selected Dhikr when the app is updated.
- Do not reset or overwrite existing saved data simply because the data structure has changed.
- Test the app with existing/older saved data.

13. DEPENDENCIES
- Do not add unnecessary dependencies.
- Reuse the existing installed packages for sound and haptics.
- The existing implementation already has the required sound/haptic capabilities.
- Avoid changing package versions unless absolutely necessary.

14. COMPLETION SOUND
- Keep the existing tap sound for normal counter presses.
- Add only the required short completion "ting"/chime as a separate sound asset.
- The completion sound should be pleasant and short.
- Do not replace or alter the existing tap sound.

15. BACKEND / UNUSED SCAFFOLDING
- Do not modify or activate the currently unused backend/API/database scaffolding.
- Keep `lib/db`, `artifacts/api-server`, and the unused API-related plumbing untouched.
- The app must remain completely local/offline.

16. EXISTING CODE STRUCTURE
- The current app is primarily implemented in `app/index.tsx`.
- Prefer making changes within the existing structure rather than unnecessarily creating a large number of new files/components.
- Keep the implementation maintainable and avoid unnecessary refactoring.

17. DO NOT MIX THIS FEATURE WORK WITH CLEANUP
Do NOT spend this task redesigning or cleaning up:
- unused counter image assets
- unused dependencies/packages
- light-theme styling
- the hard-coded "Farhaan" greeting
- the cinematic/3D design

Those will be handled separately later.

18. IMPORTANT EDGE-CASE TESTING
Specifically test:
- Existing saved data from before the update
- Switching Dhikr while counting
- Each Dhikr retaining its own count
- Each Dhikr retaining its own target
- Changing the target while a count already exists
- Reaching the target exactly
- Continuing beyond the target when Stop on Target is OFF
- Being prevented from going beyond the target when Stop on Target is ON
- Resetting and reaching the same target again
- Reaching 33, 99 and 100 when the target is higher
- Avoiding duplicate milestone + target-completion feedback
- Restarting the app and confirming all data persists

After making the changes, clearly report:
- Files modified
- Files created
- Dependencies changed (if any)
- Data structure changes
- Tests/checks performed
- Any remaining known issues

Do not make unrelated changes.