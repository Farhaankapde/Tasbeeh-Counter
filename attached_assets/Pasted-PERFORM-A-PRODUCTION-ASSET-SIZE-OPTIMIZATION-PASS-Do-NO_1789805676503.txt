PERFORM A PRODUCTION ASSET SIZE OPTIMIZATION PASS

Do NOT change the UI, layout, functionality, themes, counter behavior, navigation, or visual appearance.

The goal is ONLY to reduce unnecessary app/source size.

FIRST inspect all asset references before deleting anything.

--------------------------------------------------
1. REMOVE UNUSED DEVELOPMENT ASSETS
--------------------------------------------------

Inspect:

attached_assets/

Determine which files are actually imported/referenced by the production Tasbeeh app.

Remove old screenshots, design references, generated previews, duplicate theme concepts, and other development-only assets that are NOT imported by the application.

Do NOT remove anything that is actually used by the app.

--------------------------------------------------
2. REMOVE UNUSED ARABESQUE ASSET
--------------------------------------------------

The following asset is currently unused:

assets/images/realistic-counter-arabesque-white.png

The application uses:

assets/images/realistic-counter-arabesque-white-aligned.png

Remove the unused original Arabesque counter asset after confirming there are no references to it.

--------------------------------------------------
3. OPTIMIZE PRODUCTION IMAGES
--------------------------------------------------

Inspect all production assets in:

artifacts/tasbeeh-counter/assets/images/

Optimize large photographic/theme assets.

Prefer WebP where supported by the existing Expo/React Native image implementation.

Preserve visual quality.

Do NOT noticeably degrade:

• Counter details
• Marble texture
• Wood grain
• Gold details
• Background quality
• Islamic patterns

Target approximately 40–60% reduction in image file size where practical.

After conversion, update all imports/references correctly.

Do NOT leave both the old PNG and new WebP if the PNG is no longer required.

--------------------------------------------------
4. DO NOT OPTIMIZE APP ICONS INTO LOW QUALITY
--------------------------------------------------

Keep:

icon.png
adaptive-icon-foreground.png

at appropriate Android icon quality/resolution.

Do not aggressively compress these.

--------------------------------------------------
5. REMOVE GENERATED BUILD OUTPUT FROM SOURCE
--------------------------------------------------

Do not treat:

artifacts/tasbeeh-counter/static-build/

as permanent source assets.

If it is generated during build, ensure it is excluded from the production source/repository where appropriate.

Do not remove anything required by the actual build process.

--------------------------------------------------
6. VERIFY ALL FIVE THEMES
--------------------------------------------------

After cleanup verify:

1. Cosmic Lattice
2. Aurora Pearlescent
3. Golden Galaxy Etching
4. Sandalwood Classic
5. Arabesque White

All five themes must still load correctly.

Verify:

• Counter image
• LCD/count
• Background
• Dial/button
• Theme switching
• Persistence
• Navigation

--------------------------------------------------
7. DO NOT CHANGE VISUAL DESIGN
--------------------------------------------------

The app must look identical after optimization.

This task is ONLY:

ASSET CLEANUP + COMPRESSION + SIZE OPTIMIZATION

Do NOT redesign anything.

--------------------------------------------------
8. FINAL REPORT
--------------------------------------------------

After optimization report:

• Original production asset size
• New production asset size
• Space saved
• Files removed
• Files converted/compressed
• Confirmation that all five themes still work