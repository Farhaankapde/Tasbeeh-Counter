Fix the Expo Go runtime crash caused by the deprecated expo-av audio module.

CURRENT ERROR:
Cannot find native module 'ExponentAV'

The app currently imports:
import { Audio } from 'expo-av';

Expo Go cannot load the native expo-av module in the current environment.

Migrate the app's completion-sound implementation from expo-av to the current Expo audio package: expo-audio.

IMPORTANT:
- Do NOT change the UI.
- Do NOT change the Counter design.
- Do NOT change themes.
- Do NOT change navigation.
- Do NOT change target logic.
- Do NOT change vibration behavior.
- Do NOT remove the target-completion sound.
- Target completion MUST continue to play the existing completion.wav sound.
- Target completion MUST continue to vibrate even when normal vibration is disabled.
- Normal vibration/milestone vibration must retain their existing settings behavior.

Implementation:
1. Install the Expo-compatible expo-audio package using the project's Expo-compatible package manager/version.
2. Remove the runtime dependency on expo-av for audio playback.
3. Replace the existing Audio.Sound / expo-av implementation with the equivalent expo-audio implementation.
4. Keep the existing local completion.wav asset.
5. Keep the sound local/offline; do not introduce URLs or network audio.
6. Make sure the audio player is properly managed/released and does not leak resources.
7. Do not add microphone/recording functionality or unnecessary permissions.

IMPORTANT:
Use the expo-audio API appropriate for the project's installed Expo SDK. Do not blindly copy an old expo-av API.

After implementation:
- Run the project's tests.
- Run TypeScript/type checking.
- Verify the app starts without the ExponentAV error.
- Verify the Counter screen loads.
- Verify target completion still plays completion.wav.
- Verify target completion still vibrates.
- Verify normal counting still works.
- Verify the app still works offline.

Also verify that the route warning disappears once the application successfully loads.

Report:
- files changed
- packages added/removed
- tests/typecheck results
- whether Expo Go can now load the app

Do not make any unrelated changes.