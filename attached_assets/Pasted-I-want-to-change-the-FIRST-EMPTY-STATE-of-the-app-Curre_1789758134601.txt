I want to change the FIRST/EMPTY STATE of the app.

Currently, when there are no Dhikrs added, the Counter screen shows:
"Start your library"
"Add a Dhikr to begin your local practice."
and an "Add Dhikr" button.

I do NOT want this.

The app must function as a Tasbeeh counter even when the user has not added any Dhikr.

REQUIRED BEHAVIOR:

1. Fresh install / no Dhikrs
- Open directly to the normal Counter screen.
- Display the physical Tasbeeh counter with count = 0.
- The user must be able to press the physical metallic button and count immediately.
- Do NOT require the user to add a Dhikr first.
- Do NOT automatically create a Dhikr in the user's Dhikr Library.
- The counter should use a neutral generic state such as "Tasbeeh" or "Dhikr" when no Dhikr is selected.
- No target/progress should be shown because there is no target.
- Counting must still auto-save locally exactly as it currently does.

2. If the user later adds Dhikrs
- Keep the existing Dhikr Library behavior.
- The user can select a Dhikr and count with it.
- Existing targets, history, statistics and per-Dhikr functionality must continue working.
- Do not change the existing Dhikr data or migration behavior.

3. Empty-state text
Remove:
"Start your library"
"Add a Dhikr to begin your local practice."
and the large "Add Dhikr" empty-state button from the Counter screen.

The Counter should look like a real usable Tasbeeh immediately, not an onboarding/library screen.

4. Bottom navigation labels
Change the bottom navigation labels to:

Tasbeeh
History
Stats
Dhikrs

The first tab should be "Tasbeeh" instead of "Counter".

Keep the existing icons unless there is a strong reason they need to change.

5. Important
Do NOT redesign the entire app.
Do NOT change the cinematic visual design, physical Tasbeeh, themes, target behavior, vibration behavior, audio, auto-save, history, stats, or Dhikr Library.

Only change the empty Counter state and the bottom navigation label as described above.

After implementing, test specifically:
- Fresh install with zero Dhikrs
- Counter opens immediately
- Count can increase from 0
- App can be restarted and count is preserved
- No fake Dhikr is created
- Adding a Dhikr afterward still works normally
- Existing Dhikr users are not affected