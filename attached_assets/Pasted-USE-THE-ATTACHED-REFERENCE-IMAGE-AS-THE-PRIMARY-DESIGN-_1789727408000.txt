USE THE ATTACHED REFERENCE IMAGE AS THE PRIMARY DESIGN SOURCE.

I want the Counter screen to visually match the attached reference image as closely as possible.

Do NOT redesign it.
Do NOT interpret the design differently.
Do NOT rearrange elements.
Do NOT create your own layout.

The reference image is the target.

Keep all existing application functionality and modify ONLY the UI/layout/styling needed to match the reference.

==================================================
TARGET SCREEN STRUCTURE
=======================

The screen must have this exact vertical hierarchy:

1. Dark textured background
2. Header
3. Dhikr selector
4. Large physical red Tasbeeh counter
5. Three statistics cards
6. Daily Goal card
7. RESET / +1 / SAVE control bar
8. Bottom navigation

Everything must be visible together on a normal phone screen.

==================================================

1. BACKGROUND
   ==================================================

Use a nearly black background.

The reference has a subtle textured/brushed surface.

Use a very subtle texture/noise effect if possible.

Do NOT use a plain light background.

The application itself should fill the entire phone viewport.

==================================================
2. HEADER
=========

Position the header near the top with comfortable safe-area padding.

Left:
hamburger menu icon

Center:

Assalamu Alaikum,
Farhaan

"Assalamu Alaikum," is smaller and lighter.

"Farhaan" is larger and bold.

Right:
settings gear icon

The three areas must align horizontally.

IMPORTANT:
The greeting must NEVER overlap the phone notch/camera.

==================================================
3. DHIKR SELECTOR
=================

Below the header, create a large horizontal rounded pill.

Reference appearance:

Dark black/charcoal pill
Subtle border
Soft shadow

Inside:

LEFT:
large circular red icon containing Islamic Arabic calligraphy

CENTER:
SubhanAllah

RIGHT:
down chevron

The selector should be approximately the same width and height as the reference.

Selected Dhikr must remain functional.

==================================================
4. LARGE PHYSICAL TASBEEH COUNTER
=================================

THIS IS THE MAIN VISUAL ELEMENT.

The physical counter should occupy approximately the same proportion of the screen as shown in the reference.

It should NOT look like a normal UI card.

It should look like a REAL handheld digital Tasbeeh counter.

Use:

* glossy red outer casing
* rounded/organic body
* black inset display panel
* subtle highlights
* realistic depth
* shadows
* metallic silver controls

The counter should be centered horizontally.

Do not make it excessively narrow.

Do not make it excessively tall.

Match the reference proportions.

==================================================
5. KMA DIGITAL
==============

At the top of the physical counter's black display panel:

KMA Digital

Use a white/metallic serif font.

Center it horizontally.

It must be clearly visible.

==================================================
6. LCD DISPLAY
==============

Immediately below "KMA Digital":

Create a realistic LCD display.

Appearance:

* pale gray/green LCD background
* rounded rectangular shape
* metallic/light border
* subtle inner shadow
* realistic LCD texture

Display a 3-digit count.

Examples:

000
001
014
289
999

Use a proper 7-segment-style appearance.

The inactive segments should be faintly visible behind the active digits, like a real digital counter.

The number must come from the actual counter state.

==================================================
7. COUNT / RESET LABELS
=======================

Immediately underneath the LCD:

COUNT                         RESET

These are labels printed on the physical device.

They should be white/light gray.

Do NOT turn them into separate large buttons.

==================================================
8. PHYSICAL RESET BUTTON
========================

On the right side of the physical counter, below the display:

Small circular metallic silver button.

It should have:

* metallic appearance
* dark outline
* subtle highlight
* shadow

Keep its position similar to the reference.

It must remain functional.

==================================================
9. LARGE PHYSICAL COUNT BUTTON
==============================

The lower half of the physical counter contains one large circular metallic button.

This is the main counting button.

It should look like brushed metal.

Use several concentric layers:

outer dark shadow
metallic outer ring
silver ring
inner metallic surface

Add subtle radial/brushed-metal texture.

The button should be large and visually dominant.

Tapping it increments the count by exactly +1.

Add a subtle pressed animation.

==================================================
10. STATISTICS CARDS
====================

Immediately underneath the physical counter:

Three cards in one row.

TODAY
289

THIS WEEK
2,450

TOTAL
124,580

These are example values from the reference.

DO NOT hard-code them.

Use the actual application data.

Each card:

* dark charcoal background
* rounded corners
* subtle border
* subtle shadow
* red icon
* white label
* large white number
* small red underline

Match the proportions and spacing in the reference.

==================================================
11. DAILY GOAL
==============

Immediately underneath the three statistics cards:

One large horizontal rounded card.

Left:

red target icon

Text:

Daily Goal
1,000

Middle:

horizontal progress bar

Right:

28%
chevron

The percentage MUST be calculated dynamically.

Example:

dailyCount = 289
dailyGoal = 1000
percentage = 28.9%

Display approximately:

28%

Do not hard-code 28%.

The card should have the same dark appearance and rounded corners as the reference.

==================================================
12. RESET / +1 / SAVE BAR
=========================

Immediately underneath Daily Goal:

Create a large rounded horizontal control bar.

LEFT:

reset icon
RESET

CENTER:

large red pill button

+1

RIGHT:

save icon
SAVE

The +1 button should be significantly more prominent than RESET and SAVE.

The visual appearance should closely match the reference:

dark rounded outer container
red central button
soft shadow
white text

Functionality:

+1 → increment count

RESET → reset current count

SAVE → save/persist the current session/count using the existing application functionality

==================================================
13. BOTTOM NAVIGATION
=====================

At the very bottom:

Counter
History
Stats
Dhikrs

Four evenly spaced navigation items.

Counter is selected:

red icon
red text

Others:

gray icon
gray text

Keep the existing navigation functionality.

==================================================
14. SPACING IS CRITICAL
=======================

The reference image is a tall phone interface.

Do NOT create large empty spaces.

Do NOT allow the physical counter to push Daily Goal or RESET/+1/SAVE off the screen.

Do NOT solve this by shrinking the entire interface.

Instead, carefully tune:

* margins
* padding
* counter dimensions
* card heights
* header spacing
* selector height
* navigation height

The final screen should look dense, polished and intentional exactly like the reference.

==================================================
15. RESPONSIVE BEHAVIOR
=======================

The design should adapt to different phone sizes.

However, preserve the same visual proportions.

On a small phone:

reduce spacing slightly.

On a larger phone:

allow slightly more spacing.

Do NOT rearrange the elements.

Do NOT hide elements.

Do NOT remove Daily Goal.

Do NOT remove RESET / +1 / SAVE.

==================================================
16. IMPORTANT VISUAL RULE
=========================

The attached reference screenshot is the SOURCE OF TRUTH.

When deciding between:

"what looks good"

and

"what matches the reference"

ALWAYS choose what matches the reference.

Do not introduce additional cards, sections, buttons, colors, layouts or UI patterns that are not present in the reference.

==================================================
17. FUNCTIONALITY
=================

Preserve all existing functionality:

* counting
* physical count button
* +1
* reset
* save
* daily statistics
* weekly statistics
* total statistics
* daily goal
* Dhikr selection
* History
* Stats
* Dhikrs
* Settings
* persistence

Only change the visual presentation.

==================================================
18. FINAL VERIFICATION
======================

After implementing:

Open the app at a phone-sized viewport.

Compare it directly against the attached reference.

Verify that ALL of these are visible simultaneously:

✓ Assalamu Alaikum
✓ Farhaan
✓ Hamburger
✓ Settings
✓ Dhikr selector
✓ KMA Digital
✓ LCD counter
✓ COUNT
✓ RESET
✓ Physical reset button
✓ Large metallic count button
✓ Today
✓ This Week
✓ Total
✓ Daily Goal
✓ RESET
✓ +1
✓ SAVE
✓ Counter
✓ History
✓ Stats
✓ Dhikrs

The final result should look like the attached reference, not like a generic dashboard or generic Tasbeeh application.
