Please implement the following 10 release-readiness fixes identified in the latest Claude audit.

IMPORTANT:
- Do NOT redesign the app.
- Do NOT change existing product behavior.
- Do NOT change the themes, counter design, Dhikr behavior, target logic, milestone logic, reset logic, navigation labels, or overall UI.
- Do NOT add backend, login, analytics, networking, or cloud sync.
- Preserve the current offline/local-only architecture.

FIX 1 — Remove unused expo-image-picker
Remove the unused expo-image-picker dependency because it is not imported or used anywhere in the app.

FIX 2 — Remove unused expo-location
Remove the unused expo-location dependency because it is not imported or used anywhere in the app.

After removing both dependencies, verify that unnecessary Android permissions such as:
- ACCESS_FINE_LOCATION
- ACCESS_COARSE_LOCATION
- RECORD_AUDIO
are no longer introduced by these dependencies.

FIX 3 — Fix Android adaptive app icon safe zone
The current adaptive foreground icon artwork extends too close to the edges and can be clipped by Android adaptive icon masks.

Update/regenerate ONLY the adaptive foreground artwork so the complete prayer-bead/crescent artwork stays safely inside the central adaptive-icon safe zone.

Keep the existing Tasbeeh visual identity and appearance.
Do not redesign the icon.
Do not change the normal flat app icon unnecessarily.

FIX 4 — Disable unnecessary background audio playback
Update the expo-audio plugin configuration in app.json to explicitly set:

"enableBackgroundPlayback": false

The app only plays short tap/chime sounds and does not need background audio playback.

Do not change the existing sound effects or target-completion sound behavior.

Verify that unnecessary background media playback permissions are no longer generated.

FIX 5 — Increase Dhikr Edit/Delete touch targets
The Edit and Delete controls in each Dhikr row currently have very small visual buttons.

Increase their actual touchable area to at least 44x44 dp while keeping the visible icons approximately the same size and appearance.

Do not visually redesign the Dhikr list.

FIX 6 — Improve TalkBack accessibility for the counter
Make the current LCD counter value accessible to Android TalkBack.

The displayed count should be announced/available to accessibility services when it changes after a tap.

Use the appropriate React Native accessibility properties such as accessibilityLiveRegion and accessibilityValue where appropriate.

Do not change the visual appearance of the LCD counter.

FIX 7 — Make the Add/Edit Dhikr form keyboard-aware
The Add/Edit Dhikr modal currently uses a normal ScrollView even though the project contains KeyboardAwareScrollViewCompat.

Use the existing KeyboardAwareScrollViewCompat component for the Add/Edit Dhikr form if appropriate.

The goal is to ensure the keyboard does not cover the Create Dhikr / Save button on smaller Android screens.

Do not change the form fields, validation, or visual design.

FIX 8 — Improve Android hardware back-button navigation
Add appropriate Android BackHandler behavior for normal tab/drill-down navigation.

Desired behavior:
- If user is on History/Stats/Dhikrs, pressing Android back should return to the Tasbeeh/Counter tab instead of immediately exiting the app.
- If user is inside a History drill-down/group view, pressing back should return to the History overview first.
- Existing modal onRequestClose behavior must remain unchanged.
- If the user is already on the Counter/Tasbeeh root screen and there is nothing to go back to, allow normal Android app exit.

Do not change the visible navigation design.

FIX 9 — Remove unused image assets
Claude identified these image assets as unused:
- realistic-counter.png
- realistic-counter-smaller-dial.png
- realistic-counter-cropped.png
- realistic-counter-transparent.png

Confirm they are truly not referenced anywhere in the source code and remove them if unused.

Do not remove any image that is actually used by the app.

FIX 10 — Remove dead tabActiveIndicator style
The audit found a tabActiveIndicator style that is no longer used because the active-tab indicator line was intentionally removed.

Remove only the unused/dead style definition.

Do not reintroduce an active-tab indicator.

VERIFICATION REQUIRED

After making all 10 fixes:

1. Run the complete automated test suite.
2. Run TypeScript/type checking.
3. Run Expo prebuild or equivalent manifest generation/inspection.
4. Verify the Android manifest no longer contains the unnecessary location/audio permissions caused by the removed dependencies.
5. Verify background media playback permissions are not added by expo-audio.
6. Verify the adaptive icon foreground artwork stays within the Android safe zone.
7. Verify the Add/Edit Dhikr keyboard behavior.
8. Verify Android BackHandler behavior.
9. Verify all existing counting, target, milestone, reset, history, stats, theme, persistence, and offline functionality remains unchanged.
10. Report the exact files changed and the results of every verification.

DO NOT create a new AAB yet.
Only make the changes and verify them.